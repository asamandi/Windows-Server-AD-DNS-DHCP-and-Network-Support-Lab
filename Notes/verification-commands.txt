Verification Commands

DC1:

hostname
ipconfig /all
nslookup amir.local
net user /domain
net group /domain
dcdiag

PC1:

ipconfig /all
ping 192.168.10.10
ping DC1
nslookup amir.local
whoami
gpupdate /force
gpresult /r
net use
\\DC1\CompanyShare

Purpose:
These commands verify IP addressing, DNS resolution, domain connectivity, shared folder access, Group Policy application, and user login status.